#pragma once

namespace mcs::ABNF::URI
{
    /**
     * @brief
     *  hier-part     = "//" authority path-abempty
                 / path-absolute
                 / path-rootless
                 / path-empty
     */
};