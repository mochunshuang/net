#pragma once

namespace mcs::rfc::uri
{
    /**
     * @brief    hier-part     = "//" authority path-abempty
                 / path-absolute
                 / path-rootless
                 / path-empty
     *
     */
    struct hier_part
    {
    };

}; // namespace mcs::rfc::uri
